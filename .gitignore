# Claude Code brainstorm/scratch files
.superpowers/

# Playwright MCP session output (console logs, page snapshots) — written into
# the working directory by the browser tooling an agent drives, not by the
# repo's own Playwright suite. Runtime scratch, never worth committing.
.playwright-mcp/

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go build outputs
server/jot
server/jotctl
server/server
jot-server

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
coverage.html

# JavaScript/TypeScript test coverage
coverage/

# Go workspace file
go.work

# Node.js dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build outputs
dist/
build/
*.local

# Claude Code local settings
.claude/settings.local.json
.claude/worktrees/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Environment variables
.env
.envrc

# Database files
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Note-image blob storage (JOT_UPLOAD_DIR, default ./uploads) — runtime data
# written by a locally run server, the filesystem half of a backup alongside
# the database above.
server/uploads/
uploads/

# Mobile build artifacts
mobile/.expo/
mobile/android/
mobile/ios/
google-services.json
release.keystore

# Playwright test artifacts
webapp/playwright-report/
webapp/test-results/
test-results/
playwright-report/

.worktrees/
